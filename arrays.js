let database = ["turtle", "cat", "dog", "bird"];

console.log(database.length);
console.log(database);

console.log(database[1]);

database[0] = "dinosaur";
console.log(database);

let last = database[database.length - 1];
console.log(last);

database.push("turtle");
console.log(database);
